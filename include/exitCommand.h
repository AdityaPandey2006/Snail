#ifndef EXIT_H
#define EXIT_H
#include "parser.h"
#include "executor.h"

executorResult exitCommand(Command* newCommand); //** pouinter to pointer of character...basically a way to point to a array of strings...... */

#endif